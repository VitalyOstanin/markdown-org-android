/rust/target/
/rust/jniLibs/
/generated/

# Android / Gradle
/app/build/
/build/
/.gradle/
# Session state written by the Kotlin Gradle plugin.
/.kotlin/
local.properties
*.apk
*.aab
# Signing keys, under every name a keytool run may give them. The CI key is
# a secret and never lands here, but one generated locally would otherwise be
# offered up by `git add`.
*.keystore
*.jks
*.p12
*.pfx

# Local review output, not part of the repository.
/docs/__check/

.idea/
.DS_Store
